# Day 02

今天做了什么：

- 创建了第一个需要输入的 Python 小程序。
- 继续练习 Git 的基本流程：status、add、commit、push。

我遇到的问题：

- 还需要确认电脑上是否安装了 Python。

我还没搞懂：

- `input()` 和 `print()` 的具体作用。

明天要做：

- 如果 Python 可以运行，就运行 `hello.py`。
- 如果 Python 还不能运行，就先安装 Python。
